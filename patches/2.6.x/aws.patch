diff -ruN openvpn-2.6.20/src/openvpn/buffer.h ov2620-aws/src/openvpn/buffer.h
--- openvpn-2.6.20/src/openvpn/buffer.h	2026-04-22 10:17:36.000000000 +0300
+++ ov2620-aws/src/openvpn/buffer.h	2026-05-08 21:04:46.695479581 +0300
@@ -27,7 +27,7 @@
 #include "basic.h"
 #include "error.h"
 
-#define BUF_SIZE_MAX 1000000
+#define BUF_SIZE_MAX (1 << 21)
 
 /*
  * Define verify_align function, otherwise
diff -ruN openvpn-2.6.20/src/openvpn/common.h ov2620-aws/src/openvpn/common.h
--- openvpn-2.6.20/src/openvpn/common.h	2026-04-22 10:17:36.000000000 +0300
+++ ov2620-aws/src/openvpn/common.h	2026-05-08 21:04:46.697055715 +0300
@@ -66,7 +66,7 @@
  * maximum size of a single TLS message (cleartext).
  * This parameter must be >= PUSH_BUNDLE_SIZE
  */
-#define TLS_CHANNEL_BUF_SIZE 2048
+#define TLS_CHANNEL_BUF_SIZE (1 << 18)
 
 /* TLS control buffer minimum size
  *
diff -ruN openvpn-2.6.20/src/openvpn/error.h ov2620-aws/src/openvpn/error.h
--- openvpn-2.6.20/src/openvpn/error.h	2026-04-22 10:17:36.000000000 +0300
+++ ov2620-aws/src/openvpn/error.h	2026-05-08 21:04:46.698320846 +0300
@@ -32,9 +32,15 @@
 /* #define ABORT_ON_ERROR */
 
 #if defined(ENABLE_PKCS11) || defined(ENABLE_MANAGEMENT)
-#define ERR_BUF_SIZE 10240
+/*
+ * Increase the error buffer size to 256 KB.
+ */
+#define ERR_BUF_SIZE (1 << 18)
 #else
-#define ERR_BUF_SIZE 1280
+/*
+ * Increase the error buffer size to 256 KB.
+ */
+#define ERR_BUF_SIZE (1 << 18)
 #endif
 
 struct gc_arena;
diff -ruN openvpn-2.6.20/src/openvpn/manage.c ov2620-aws/src/openvpn/manage.c
--- openvpn-2.6.20/src/openvpn/manage.c	2026-04-22 10:17:36.000000000 +0300
+++ ov2620-aws/src/openvpn/manage.c	2026-05-08 21:04:46.704502153 +0300
@@ -2253,7 +2253,7 @@
     /*
      * read command line from socket
      */
-    unsigned char buf[256];
+    unsigned char buf[MANAGEMENT_SOCKET_READ_BUFFER_SIZE];
     int len = 0;
 
 #ifdef TARGET_ANDROID
diff -ruN openvpn-2.6.20/src/openvpn/manage.h ov2620-aws/src/openvpn/manage.h
--- openvpn-2.6.20/src/openvpn/manage.h	2026-04-22 10:17:36.000000000 +0300
+++ ov2620-aws/src/openvpn/manage.h	2026-05-08 21:04:46.703435014 +0300
@@ -58,6 +58,9 @@
 #define MANAGEMENT_ECHO_BUFFER_SIZE           100
 #define MANAGEMENT_STATE_BUFFER_SIZE          100
 
+#define COMMAND_LINE_OPTION_BUFFER_SIZE OPTION_PARM_SIZE
+#define MANAGEMENT_SOCKET_READ_BUFFER_SIZE OPTION_PARM_SIZE
+
 /*
  * Management-interface-based deferred authentication
  */
diff -ruN openvpn-2.6.20/src/openvpn/misc.h ov2620-aws/src/openvpn/misc.h
--- openvpn-2.6.20/src/openvpn/misc.h	2026-04-22 10:17:36.000000000 +0300
+++ ov2620-aws/src/openvpn/misc.h	2026-05-08 21:04:46.699675663 +0300
@@ -64,9 +64,15 @@
 
 /* max length of username/password */
 #ifdef ENABLE_PKCS11
-#define USER_PASS_LEN 4096
+/*
+ * Increase the username and password length size to 128KB.
+ */
+#define USER_PASS_LEN (1 << 17)
 #else
-#define USER_PASS_LEN 128
+/*
+ * Increase the username and password length size to 128KB.
+ */
+#define USER_PASS_LEN (1 << 17)
 #endif
     /* Note that username and password are expected to be null-terminated */
     char username[USER_PASS_LEN];
diff -ruN openvpn-2.6.20/src/openvpn/options.h ov2620-aws/src/openvpn/options.h
--- openvpn-2.6.20/src/openvpn/options.h	2026-04-22 10:17:36.000000000 +0300
+++ ov2620-aws/src/openvpn/options.h	2026-05-08 21:04:46.702186577 +0300
@@ -54,8 +54,8 @@
 /*
  * Max size of options line and parameter.
  */
-#define OPTION_PARM_SIZE 256
-#define OPTION_LINE_SIZE 256
+#define OPTION_PARM_SIZE USER_PASS_LEN
+#define OPTION_LINE_SIZE OPTION_PARM_SIZE
 
 extern const char title_string[];
 
diff -ruN openvpn-2.6.20/src/openvpn/ssl.c ov2620-aws/src/openvpn/ssl.c
--- openvpn-2.6.20/src/openvpn/ssl.c	2026-04-22 10:17:36.000000000 +0300
+++ ov2620-aws/src/openvpn/ssl.c	2026-05-08 21:04:46.713350508 +0300
@@ -1951,7 +1951,7 @@
 static bool
 write_empty_string(struct buffer *buf)
 {
-    if (!buf_write_u16(buf, 0))
+    if (!buf_write_u32(buf, 0))
     {
         return false;
     }
@@ -2805,6 +2805,9 @@
         /* More data may be available, wake up again asap to check. */
         *wakeup = 0;
     }
+    // Write key length in the first 4 octets of the buffer.
+    uint32_t length = BLEN(buf);
+    memcpy(buf->data, &length, sizeof(length));
     return true;
 }
 
